# 项目规则索引

**细则正文**均在 `.rules/`。`CLAUDE.md` 与 `.cursor/rules/` 仅指针。

---

## 按任务必读

| 任务 | 必读 |
|------|------|
| 任意 | [`00-collaboration.md`](00-collaboration.md) |
| 写代码 / 改 WebUI | + 读 [`README.md`](../README.md) 了解架构；配置见 [`config.example.toml`](../config.example.toml) |

---

## 规则文件索引

| § | 文件 | 何时必读 |
|---|------|----------|
| 0 | [`00-collaboration.md`](00-collaboration.md) | 协作铁律（中文沟通 / 中文 commit） |

维护：新增规范写入 `.rules/*.md` 并同步本索引；勿在 `CLAUDE.md` 或 `.cursor/rules/` 堆细则。
